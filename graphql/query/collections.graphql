query LatestCollections($offset: Int!, $limit: Int!) {
  collections(offset: $offset, limit: $limit, sortBy: MINTED_AT_DESC) {
    collections {
      collectionAddr
      name
      image
      description
      mintedAt
    }
  }
}
